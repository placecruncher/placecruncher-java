package com.placecruncher.server.dao;


public class PrincipalDao /* extends AbstractDao<Integer, Principal> */ {
/*
    public PrincipalDao() {
        super(Principal.class);
    }

    public Principal findByToken(String token) {
        // This will need to be made much smarter.
        Query query = createQuery("from Principal p where p.token = :token");
        query.setString("token", token);
        return (Principal) query.uniqueResult();
    }
    
    public Principal findByUserNameAndPassword(String userName, String password) {
        // This will need to be made much smarter.
        Query query = createQuery("from Principal p where p.username = :username and p.password = :password");
        query.setString("username", userName);
        query.setString("password", password);
        return (Principal) query.uniqueResult();
    }
    */
}
